#include <iostream>
#include <vector>
using namespace std;

int main()
{
    int a = 0, b = 0, c =0;


    vector<vector<int> > arr {};
    //arr.emplace_back(vector<int>{a, b, c});
    arr.emplace_back(vector<int>{a, b, c});
    return 0;
}
